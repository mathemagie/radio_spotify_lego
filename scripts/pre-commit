#!/bin/sh
set -eu

if [ ! -x ".venv/bin/ruff" ] || [ ! -x ".venv/bin/black" ]; then
    echo "Ruff and Black are required. Run: make install-dev" >&2
    exit 1
fi

.venv/bin/ruff check .
.venv/bin/black --check .
